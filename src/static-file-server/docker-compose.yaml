---
version: "3.9"

services:
  static-file-server:
    image: halverneus/static-file-server:v1.8.6
    container_name: static-file-server
    volumes:
      - ./site:/web
    networks:
      - caddy
    restart: unless-stopped

networks:
  caddy:
    external: true
