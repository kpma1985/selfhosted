---
version: "3.9"

services:
  sonarr:
    image: ghcr.io/hotio/sonarr:v4-4.0.0.316
    container_name: sonarr
    restart: unless-stopped
    environment:
      - PUID=1000
      - PGID=1000
      - UMASK=002
      - TZ=America/Chicago
    volumes:
      - ~/selfhosted/data/sonarr:/config
    networks:
      - caddy # upstreams 8989

  radarr:
    image: ghcr.io/hotio/radarr:release-4.2.4.6635
    container_name: radarr
    restart: unless-stopped
    environment:
      - PUID=1000
      - PGID=1000
      - UMASK=002
      - TZ=America/Chicago
    volumes:
      - ~/selfhosted/data/radarr:/config
    networks:
      - caddy # upstreams 7878

  prowlarr:
    image: ghcr.io/hotio/prowlarr:nightly-1.0.0.2171
    container_name: prowlarr
    restart: unless-stopped
    environment:
      - PUID=1000
      - PGID=1000
      - UMASK=002
      - TZ=America/Chicago
    volumes:
      - ~/selfhosted/data/prowlarr:/config
    networks:
      - caddy # upstreams 9696

networks:
  caddy:
    external: true
